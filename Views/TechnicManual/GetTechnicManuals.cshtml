@model IEnumerable<CourseWorkFactoryAutomatization.Models.TechnicManual>

@{
    ViewData["Title"] = "GetTechnicManuals";
    Layout = "_Layout_SuperVisor";
}

<h1>GetTechnicManuals</h1>

<p>
    <a asp-action="CreateTechnicManual">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateCreate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreate)
            </td>
            <td>
                    <a asp-action="EditTechnicManual" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="InfoTechnicManual" asp-route-id="@item.Id">Info</a> |
                    <a asp-action="DeleteTechnicManual" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
