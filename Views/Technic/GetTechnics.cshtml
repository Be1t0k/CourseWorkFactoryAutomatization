@model IEnumerable<CourseWorkFactoryAutomatization.Models.Technic>

@{
    ViewData["Title"] = "GetTechnics";
}

<h1>GetTechnics</h1>

<p>
    <a asp-action="CreateTechnic">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TechnicManual)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <th>
                    @Html.DisplayFor(modelItem => item.TechnicManualId)
            </th>
            <td>
                <a asp-action="EditTechnic" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="InfoTechnics" asp-route-id="@item.Id">Info</a> |
                <a asp-action="DeleteTechnic" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
