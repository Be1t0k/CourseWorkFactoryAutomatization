@model IEnumerable<CourseWorkFactoryAutomatization.Models.UserCatalog>

@{
    ViewData["Title"] = "GetUserCatalogs";
}

<h1>GetUserCatalogs</h1>

<p>
    <a asp-action="CreateUserCatalog">Создать новый</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateCreate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreate)
            </td>
            <td>
                    <a asp-action="EditUserCatalog" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="InfoUserCatalog" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeleteUserCatalog" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
